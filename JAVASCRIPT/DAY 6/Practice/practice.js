//1-> to input default name guest123 in username age 18 when nothing is given input by user using || 
/*
// when we will enter nothing in username  and age then it returns null and NaN but in below code if we enter nothig then it give  default values Gues123  and to   them
const username = prompt("Enter name") || "Guest123"
const  age = parseInt(prompt("Enter age")  || 18)) //promt returns string so it is converted to number.however,Js itself converts string to number but it is best practice 

console.log(`Your username is ${username} and age is ${age}`)
*/



